// AURA — Google account backup configuration.
//
// This file is intentionally left with placeholder values. Until you
// replace them with your own Firebase project's config, the app runs
// exactly as before — fully local-only, "Sign in with Google" just
// shows a friendly message instead of erroring.
//
// These values are NOT secret — Firebase's client config is meant to
// be public (real security lives in the Firestore rules you'll set
// up in the README). It's safe to commit this file to a public repo.
//
// How to fill this in — see the "Account backup (Google sign-in)"
// section in README.md for the full step-by-step.

window.AURA_FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
